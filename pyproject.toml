[project]
name = "norabradford-site"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pillow>=12.3.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.1",
]

[tool.uv]
package = false
